# Procurement Knowledge Base — MCP Server

A local data pipeline and MCP server that allows AI agents to retrieve, reason over, and explain information from a procurement document knowledge base.

Built as a take-home exercise for Caseware. Designed to be simple, local, and maintainable — not overengineered.

---

## What it does

Ingests 45 procurement documents (invoices, purchase orders, shipping orders, inventory reports, and one contract), extracts structured fields and full text, stores everything in a local SQLite database, and exposes 6 MCP tools that an AI agent can use to answer audit questions like:

- *"Which invoices are missing a matching purchase order?"*
- *"What documents support order 10687?"*
- *"Does this shipment match its related invoice?"*
- *"What are the contract terms for supply of goods?"*
- *"What inventory reports are available and what period do they cover?"*

---

## Stack

| Tool | Purpose |
|---|---|
| `pdfplumber` | Text extraction from text-based PDFs |
| `pytesseract` + `pdf2image` | OCR for image-based invoices (JPGs) |
| `SQLite` (built-in) | Document storage and metadata |
| `SQLite FTS5` | Full-text search across all documents |
| `FastMCP` | MCP server framework |
| `Python 3.12` | Runtime |

No cloud services. No vector database. No Docker. Runs entirely on localhost.

---

## Project structure

```
procurement-mcp/
├── pipeline.py          # Ingestion: extract → parse → store → link
├── mcp_server.py        # MCP server: 6 tools over SQLite
├── requirements.txt     # Python dependencies
├── README.md
└── db/
    ├── knowledge_base.db    # SQLite database (generated by pipeline)
    └── extracted/           # Plain text dumps per document (lineage)
```

---

## Setup

### 1. Prerequisites

- Python 3.12+
- Tesseract OCR — download from https://github.com/UB-Mannheim/tesseract/wiki
  - Windows: install to `C:\Program Files\Tesseract-OCR` and add to PATH

### 2. Install dependencies

```bash
python -m venv venv

# Windows
.\venv\Scripts\Activate.ps1

# macOS / Linux
source venv/bin/activate

pip install -r requirements.txt
```

### 3. Run the data pipeline

Point `--data` at the root folder containing the five document category subfolders:

```bash
python pipeline.py --data /path/to/data
```

Expected output:
```
── invoices ──
  ✓ invoices/invoice_10248 | order=10248 | method=pdfplumber
  ✓ invoices/batch1-1486   | order=232855 | method=tesseract_image
  ...

Materializing cross-document relationships...
  Invoices missing a PO:   ['invoice_10436.pdf', 'invoice_10687.pdf', ...]
  POs missing an invoice:  ['purchase_orders_10372.pdf', ...]

✅ Pipeline complete — 45 documents ingested into db/knowledge_base.db
```

### 4. Run the MCP server

```bash
python mcp_server.py
```

The server starts on stdio transport and is ready for MCP client connections.

---

## Connecting to Claude Desktop

Add the following to your `claude_desktop_config.json`:

**macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`  
**Windows (Store app):** `%LOCALAPPDATA%\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\claude_desktop_config.json`

```json
{
  "mcpServers": {
    "procurement-knowledge-base": {
      "command": "python",
      "args": ["/absolute/path/to/mcp_server.py"]
    }
  }
}
```

Restart Claude Desktop. The 6 tools will appear under Extensions.

---

## MCP Tools

| Tool | Description |
|---|---|
| `search_documents` | Full-text search across all documents, with optional category filter |
| `get_document` | Retrieve full text and metadata for a single document by ID |
| `match_order` | Find all documents linked to a specific order ID across all categories |
| `find_mismatches` | Find documents missing an expected counterpart (e.g. invoice without a PO) |
| `list_inventory_periods` | List all inventory reports and the date periods they cover |
| `get_contract_terms` | Search for specific terms or clauses within contract documents |

Each tool returns structured results with `source` (filename) and `category` fields for full traceability back to the original document.

---

## Running the test suite

A programmatic test script validates all 8 MCP tools directly without requiring a running MCP client:

```bash
python test_tools.py
```

Expected output:
```
── search_documents ──────────────────────────────────────────
  PASS  search_documents: finds vendor by name
  PASS  search_documents: category filter works
── get_document ──────────────────────────────────────────────
  PASS  get_document: retrieves known invoice
  PASS  get_document: returns error for unknown doc
── match_order ───────────────────────────────────────────────
  PASS  match_order: order 10248 has invoice + PO + shipment
  PASS  match_order: order 10687 missing PO
── find_mismatches ───────────────────────────────────────────
  PASS  find_mismatches: finds invoices without PO
  PASS  find_mismatches: finds POs without invoice
── list_inventory_periods ────────────────────────────────────
  PASS  list_inventory_periods: returns 7 reports spanning 2016–2018
── get_contract_terms ────────────────────────────────────────
  PASS  get_contract_terms: finds delivery terms in contract
── get_related_documents ─────────────────────────────────────
  PASS  get_related_documents: invoice_10248 has related docs
  PASS  get_related_documents: batch invoice with no order_id handled gracefully
── search_by_vendor ──────────────────────────────────────────
  PASS  search_by_vendor: finds TotalEnergies contract
  PASS  search_by_vendor: finds Hungry Owl documents
───────────────────────────────────────────────────────
  Results: 14 passed, 0 failed out of 14 tests
───────────────────────────────────────────────────────
```

Each test validates a specific capability:

| Test | What it validates |
|---|---|
| `search_documents: finds vendor by name` | FTS search returns results for a known vendor |
| `search_documents: category filter works` | Category parameter correctly scopes results |
| `get_document: retrieves known invoice` | Document lookup by ID returns correct order and category |
| `get_document: returns error for unknown doc` | Graceful error handling for missing documents |
| `match_order: order 10248 has invoice + PO + shipment` | Full match — all three document types present |
| `match_order: order 10687 missing PO` | Partial match — correctly identifies missing PO |
| `find_mismatches: finds invoices without PO` | Mismatch detection returns results |
| `find_mismatches: finds POs without invoice` | Reverse mismatch detection works |
| `list_inventory_periods: returns 7 reports spanning 2016–2018` | All inventory reports ingested with correct date range |
| `get_contract_terms: finds delivery terms in contract` | FTS finds relevant passage in contract body |
| `get_related_documents: invoice_10248 has related docs` | Cross-document linking via shared order ID works |
| `get_related_documents: batch invoice with no order_id handled gracefully` | Edge case — null order ID handled without error |
| `search_by_vendor: finds TotalEnergies contract` | Vendor search finds contract document |
| `search_by_vendor: finds Hungry Owl documents` | Vendor search finds transactional documents |

---

## Architecture overview

```
Raw files (PDFs + JPGs)
       │
       ▼
Format detection
  ├── Text PDF → pdfplumber
  └── Image / JPG → pdf2image + pytesseract (OCR)
       │
       ▼
Field extraction (regex)
  order_id · vendor · date · amount · period
       │
       ▼
SQLite storage
  ├── documents table (metadata + raw_text)
  ├── documents_fts (FTS5 full-text index)
  └── document_links (pre-computed order_id joins)
       │
       ▼
Relationship materialization
  has_matching_po · has_matching_invoice · has_matching_shipment
       │
       ▼
MCP Server (FastMCP, stdio)
  └── 6 tools → AI agent queries
```

---

## Data modeling decisions

**Order ID as join key.** Invoices, purchase orders, and shipping orders all share numeric order IDs in their filenames and document bodies. Cross-document matching is a simple equi-join on `order_id` — no fuzzy matching needed.

**Pre-computed mismatch flags.** The pipeline materializes `has_matching_po`, `has_matching_invoice`, and `has_matching_shipment` flags at ingestion time. The MCP tools read these flags directly rather than computing set logic at query time — keeping tool response times fast and the server stateless.

**SQLite FTS5 over vector search.** The dataset is 45 documents. SQLite's built-in full-text search handles all keyword and phrase queries at this scale. A vector database would add a 500MB+ model download, non-deterministic results, and significant setup complexity — unjustifiable for this dataset size.

**Plain text lineage.** Every document's extracted text is saved to `db/extracted/` as a `.txt` file. This makes it easy to inspect what the pipeline extracted and validate OCR quality without querying the database.

---

## Retrieval strategy

**Structured queries** (order ID lookups, mismatch detection) → SQLite direct queries on indexed fields. Used by `match_order` and `find_mismatches`.

**Full-text search** (vendor names, product terms, contract clauses) → SQLite FTS5 virtual table. Used by `search_documents` and `get_contract_terms`.

**Passage extraction** (contract terms) → After FTS match, the server finds the nearest passage around the query term and returns a 400-character window with context. This avoids returning raw multi-page contract dumps.

**Tradeoff acknowledged:** FTS5 is keyword-based — it won't match synonyms or paraphrases. For a production system, a hybrid approach (keyword + semantic vector search) would improve recall on the contract and inventory documents. For 45 documents and a 3–5 hour exercise, FTS5 is the right call.

---

## Known limitations

- **5 batch JPG invoices** (batch1-1486, batch1-1488, batch2-0998/0999/1000) have noisy OCR output. Order IDs could not be reliably extracted from 3 of them due to handwritten or degraded text. They are stored with `order_id = NULL` and flagged as unmatched.
- **Contract order ID** was incorrectly parsed from the filename date (20231231 → 20231). Contracts are not order-linked by design and are queried via `get_contract_terms` instead.
- **Vendor extraction** is regex-based and may miss vendors with non-standard formatting. A production pipeline would use a dedicated NER model.

---

## Structured extraction vs document retrieval

This system uses a **hybrid approach** depending on the document type and query:

**Structured extraction** is used for fields that appear consistently across documents — order ID, vendor name, date, amount, period. These are parsed at ingestion time via regex and stored as indexed columns. Queries like "which invoices are missing a PO?" run as pure SQL joins on these fields — fast, deterministic, and explainable.

**Document retrieval** (FTS5) is used for unstructured content — contract clauses, inventory descriptions, vendor mentions in body text. When a query can't be answered from structured fields alone, the FTS index scans extracted raw text across all documents.

The decision boundary is simple: if the answer lives in a known field, use structured extraction. If the answer requires reading the document, use full-text retrieval. The `get_contract_terms` tool is a good example of the latter — contracts don't have structured fields, so the tool searches raw text and returns the most relevant passage with surrounding context.

---

## Future improvements

Given more time, the following would meaningfully improve the system:

**Semantic / hybrid search.** FTS5 is keyword-based and won't match synonyms or paraphrases. Adding a lightweight embedding model (e.g. `sentence-transformers/all-MiniLM-L6-v2`) alongside FTS5 would improve recall for contract and inventory queries where exact keyword matches may not exist. A hybrid BM25 + vector re-rank approach would be the right next step.

**Better OCR post-processing.** The 5 JPG invoices produced noisy OCR output. A post-processing step using regex normalization and confidence thresholds would improve field extraction accuracy on scanned documents. For production, a dedicated document AI model (e.g. Azure Document Intelligence) would handle these more reliably.

**Vendor entity resolution.** Vendor names are inconsistently formatted across documents. A simple fuzzy matching step (e.g. `rapidfuzz`) at ingestion time would normalize vendor names and improve `search_by_vendor` recall significantly.

**Richer relationship graph.** The current `document_links` table joins only on order ID. A more complete graph would also link documents by vendor, date range, and line item — enabling queries like "show all documents related to this vendor in Q4 2017."

**Pipeline incremental updates.** The current pipeline re-processes all documents on every run. An incremental mode that only processes new or modified files would be important for a growing document corpus.

**MCP resources in addition to tools.** The current server exposes only tools. Adding MCP resources (e.g. a browsable document index) would give AI agents a way to explore the knowledge base without needing to know specific query terms upfront.

---

## AI assistance

This project was built with Claude (claude.ai) as the primary AI development tool.

Claude was used for: architecture design, stack selection and tradeoff analysis, pipeline and MCP server code generation, and README drafting.

Validation was done by: running the pipeline against the actual documents and reviewing extraction output, verifying the mismatch report against manual inspection of filenames, and testing all 6 MCP tools via Claude Desktop with the example questions from the spec.